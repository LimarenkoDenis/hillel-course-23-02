import { HightlithDirective } from './hightlith.directive';

describe('HightlithDirective', () => {
  it('should create an instance', () => {
    const directive = new HightlithDirective();
    expect(directive).toBeTruthy();
  });
});
